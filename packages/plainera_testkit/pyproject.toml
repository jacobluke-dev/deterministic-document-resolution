[tool.poetry]
name = "plainera-testkit"
version = "0.1.0"
packages = [{ include = "test_kit" }]

[tool.poetry.dependencies]
python = "^3.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
