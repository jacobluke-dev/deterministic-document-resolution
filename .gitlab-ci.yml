# Pipelines only for non-draft MRs and default branch
workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_DRAFT == "true"  # draft MR
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"                                       # non-draft MR
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH                                            # default branch
    - when: never

stages: [core]

variables:
  ENVIRONMENT: "CI"

cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - .cache/pip
    - .cache/pypoetry

core:
  stage: core
  image: python:3.13
  variables:
    POETRY_VERSION: "1.8.3"
    POETRY_VIRTUALENVS_CREATE: "false"
    PIP_DISABLE_PIP_VERSION_CHECK: "1"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_DRAFT == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - packages/plainera_core/**/*
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes:
        - packages/plainera_core/**/*
    - when: never
  before_script:
    - python --version
    - pip install "poetry==$POETRY_VERSION"
    - cd packages/plainera_core
    - poetry install --no-interaction
  script:
    - make lint
    - make typecheck
    - make test
  artifacts:
    when: always
    paths:
      - packages/plainera_core/coverage.xml
    reports:
      coverage_report:
        coverage_format: cobertura
        path: packages/plainera_core/coverage.xml
    expire_in: 1 week

api:
  stage: core
  image: python:3.13
  services:
    - name: postgres:15-alpine
      alias: db
      command: ["postgres", "-c", "log_statement=none"]
  variables:
    POETRY_VERSION: "1.8.3"
    POETRY_VIRTUALENVS_CREATE: "false"
    PIP_DISABLE_PIP_VERSION_CHECK: "1"
    POSTGRES_DB: "unacronym"
    POSTGRES_USER: "unacronym"
    POSTGRES_PASSWORD: "test-password"
    DATABASE_URL: "postgresql+psycopg://unacronym:test-password@db:5432/unacronym"
    APP_ENV: "test"
    AUTH_DISABLED: "false"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_DRAFT == "true"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - services/unacronym_api/**/*
        - packages/plainera_core/**/*
        - packages/plainera_observability/**/*
        - packages/plainera_testkit/**/*
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes:
        - services/unacronym_api/**/*
        - packages/plainera_core/**/*
        - packages/plainera_observability/**/*
        - packages/plainera_testkit/**/*
    - when: never
  before_script:
    - python --version
    - pip install "poetry==$POETRY_VERSION"
    - cd services/unacronym_api
    # Make all local packages importable for tests/fixtures
    - export PYTHONPATH="$PWD/src:$CI_PROJECT_DIR/packages/plainera_core/src:$CI_PROJECT_DIR/packages/plainera_observability:$CI_PROJECT_DIR/packages/plainera_testkit:$PYTHONPATH"
    - poetry install --no-interaction --no-root
  script:
    # Wait for DB
    - |
      poetry run python - <<'PY'
      import os, time, sys, psycopg
      url = os.environ["DATABASE_URL"].replace("+psycopg", "")
      for _ in range(60):
          try:
              with psycopg.connect(url, connect_timeout=2) as conn:
                  with conn.cursor() as cur: cur.execute("SELECT 1")
              print("DB is ready"); sys.exit(0)
          except Exception:
              time.sleep(1)
      print("DB not ready in time", file=sys.stderr); sys.exit(2)
      PY
    - poetry run alembic -c alembic.ini upgrade head
    - make lint
    - make typecheck
    # Ensure coverage.xml exists for artifacts
    - poetry run pytest --cov=public_api --cov-report=term-missing --cov-report=xml:coverage.xml
  artifacts:
    when: always
    paths:
      - services/unacronym_api/coverage.xml
    reports:
      coverage_report:
        coverage_format: cobertura
        path: services/unacronym_api/coverage.xml
    expire_in: 1 week
